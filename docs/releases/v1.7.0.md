# 🏛️ Release Notes — v1.7.0: Simplified Single-Row Ribbon, Core Native I/O & Studio UI Polish

**Ngày phát hành:** 18/08/2026  
**Dự án:** Document Converter Tool  
**Phiên bản:** `v1.7.0`  
**Nhánh:** `feat/duy-18082026-simplified-ribbon`  

---

## 🚀 Highlights & Tính Năng Nổi Bật

### 🎨 1. Thanh Simplified Single-Row Ribbon Bar (38–40px)
- **Thu gọn diện tích dọc:** Chuyển đổi toàn diện từ mô hình 2 tầng (Tab Strip + 60px panel = ~100px) sang **1 hàng ngang duy nhất cao 38–40px** đặt ngay phía trên Editor, giải phóng hơn 60% không gian làm việc cho vùng soạn thảo.
- **Bố cục 5 Nhóm Công Cụ Chuẩn UX (4/8dp Rhythm):**
  - **Nhóm File:** Logo DocConvert + Nút Mở tệp (`ft.Icons.FILE_OPEN_ROUNDED`), Lưu tệp (`ft.Icons.SAVE_OUTLINED`), Xóa nội dung (`ft.Icons.DELETE_OUTLINED`).
  - **Nhóm Typography:** Compact Heading Selector (Bình thường / H1–H6), In đậm (`BOLD`), In nghiêng (`ITALIC`), Gạch ngang (`FORMAT_STRIKETHROUGH`), Khối mã (`CODE`).
  - **Nhóm Đoạn văn & Đa phương tiện:** Trích dẫn khối (`FORMAT_QUOTE`), Danh sách không thứ tự (`FORMAT_LIST_BULLETED`), Danh sách có thứ tự (`FORMAT_LIST_NUMBERED`), Trình tạo bảng (`TABLE_CHART_OUTLINED`), Chèn ảnh (`IMAGE_OUTLINED`).
  - **Nhóm Tìm kiếm:** Nút Tìm & Thay thế (`ft.Icons.SEARCH_ROUNDED`) mở trượt panel tìm kiếm khi cần.
  - **Nhóm Điều khiển Phải:** Menu chọn Chế độ chuyển đổi (Conversion Mode), Nút bật/tắt Xem trước (`btn_tab_view_preview`), Nút bật/tắt Đường dẫn (`btn_tab_view_pathbar`), Cài đặt (`SETTINGS_OUTLINED`) và Trợ giúp (`HELP_OUTLINE_ROUNDED`).
- **Phản Hồi Thị Giác Động (Dynamic Visual State Toggles):**
  - Khi **BẬT (Active)**: Icon chuyển sang dạng Filled/Rounded (`PREVIEW_ROUNDED`, `ALT_ROUTE_ROUNDED`) và sáng màu **Accent Primary** theo Palette đang chọn.
  - Khi **TẮT (Inactive)**: Icon chuyển sang dạng Outlined (`PREVIEW_OUTLINED`, `ROUTE_OUTLINED`) với màu Muted Grey.
- **Tương Thích Ngược An Toàn (Zero-Breakage Backward Compatibility):**
  - Bổ sung các phương thức wrapper an toàn: `select_tab()`, `_select_tab()`, `deselect_all_tabs()` giúp các controller cũ (`FileController`, `WorkspaceView`, `App`) tiếp tục vận hành hoàn hảo không phát sinh lỗi.

---

### 📂 2. Bộ Hàm Chọn Thư Mục Hệ Thống (Folder Native Picker)
- **Tích hợp `pick_directory_sync()` & `pick_directory_async()` trong [`native_dialogs.py`](file:///c:/Users/Admin/Desktop/DocumentConvertTool/src/ui_flet/native_dialogs.py)**:
  - Sử dụng `tkinter.filedialog.askdirectory()` với cờ High-DPI Per-Monitor v2 (`SetProcessDpiAwarenessContext(-4)`).
  - Hỗ trợ bất đồng bộ qua `asyncio.to_thread` không gây nghẽn luồng giao diện Flet.
  - Sẵn sàng làm nền tảng cho tính năng mở Thư mục Dự án và Activity Bar ở phiên bản **v1.8.0**.

---

### 📏 3. Tinh Gọn File Path Bar & Search Bar Siêu Mỏng
- **Single-Row File Path Bar ([`file_path_bar.py`](file:///c:/Users/Admin/Desktop/DocumentConvertTool/src/ui_flet/components/file_path_bar.py)):**
  - Chuyển từ mô hình xếp chồng 2 hàng dọc (~90px) sang **1 hàng ngang chia đôi 50/50** (~38px) kèm đường chia dọc `ft.VerticalDivider`.
  - Nửa trái: Đường dẫn tệp đầu vào (`in_path_text`) + Nút mở tệp (`FOLDER_OPEN_ROUNDED`).
  - Nửa phải: Đường dẫn tệp đầu ra (`out_path_text`) + Nút lưu tệp (`SAVE_OUTLINED`).
  - Loại bỏ nền xám thô cứng của nút Browse, chuyển sang viền bo `6px`, padding `4x4px` và tự động đồng bộ màu Accent theo Palette.
- **Ultra-Compact Search & Replace Bar ([`search_replace_bar.py`](file:///c:/Users/Admin/Desktop/DocumentConvertTool/src/ui_flet/components/search_replace_bar.py)):**
  - Căn chỉnh input fields: `text_size=12`, `dense=True`, border radius `6px`.
  - Nút điều hướng chuyển về `icon_size=16`, nút Thay thế thu gọn padding `10x4px`, font `12px`.
  - Giảm hơn **50% chiều cao** panel tìm kiếm khi kích hoạt.

---

### 📖 4. Tái Thiết Kế Help View Sang Card Grid 2 Cột & Custom FAQ Accordion
- **Bố Cục Card Grid 2 Cột ([`help_view.py`](file:///c:/Users/Admin/Desktop/DocumentConvertTool/src/ui_flet/views/help_view.py)):**
  - **Cột Trái:**
    - *Card 1 — Phím tắt bàn phím:* Bảng phím tắt chi tiết với phím bấm Consolas bo góc nổi bật.
    - *Card 2 — Cú pháp Markdown nhanh:* Bảng tra cứu cú pháp H1–H6, Bold, Italic, Strike, Code, Quote, Table, Image.
  - **Cột Phải:**
    - *Card 3 — Ma trận định dạng đầy đủ:* Bảng hỗ trợ đọc/ghi kèm 9 định dạng tệp (.md, .docx, .xlsx, .csv, .pdf, .html, .json, .yaml, .pptx).
    - *Card 4 — Mẹo & Tính năng nổi bật:* 4 khối Pro-tips về Autosave Recovery, 4-Palette Themes, Base64 Image Preview, Regex Search.
- **Custom FAQ Accordion (`FaqItem`):**
  - Căn lề trái 100%, câu trả lời thụt lề chuẩn 31px thẳng hàng dưới câu hỏi.
  - Hiệu ứng mở/đóng mượt mà với icon mũi tên xoay sáng màu Accent.
- **Làm sạch Đa Ngôn Ngữ (i18n):**
  - Xóa sạch toàn bộ emoji text trùng lặp trong file từ điển `vi.json` và `en.json`.

---

### 🔤 5. Chuẩn Hóa Hệ Thống Cỡ Chữ (Studio Typography Scale)
- **Tiêu đề View (Settings & Help):** Đưa về `size=16`, `FontWeight.BOLD`, icon `18px`.
- **Thanh điều hướng Sidebar (Settings):** Chiều rộng `160px`, item font `12px`, icon `16px`, bo góc `6px`, padding `10x6px`.
- **Tiêu đề Panel (Editor & Preview Header):** `size=12`, `FontWeight.W_600`, icon `16px`, divider `1px`.
- **Thanh Footer Bar:** Hạ chiều cao xuống chuẩn Status Bar ~38px, nút Chuyển đổi gọn gàng với padding `14x6px`, font `12px`.

---

## 🧪 Kết Quả Kiểm Thử (Verification)

```powershell
python -m unittest discover -s tests -p "test_*.py"
```
```text
Ran 60 tests in 4.116s
OK
[SUCCESS] TestOverwriteDialog passed cleanly!
```

---

## 📁 Danh Sách File Ảnh Hưởng
- `src/__version__.py`: Bump phiên bản lên `1.7.0`.
- `src/ui_flet/layout/ribbon_bar.py`: Tái cấu trúc Single-Row Ribbon (38-40px) và dynamic toggle states.
- `src/ui_flet/layout/footer_bar.py`: Tối ưu chiều cao Status Bar và nút bấm Footer.
- `src/ui_flet/components/file_path_bar.py`: Tinh gọn thanh đường dẫn thành 1 hàng 50/50.
- `src/ui_flet/components/search_replace_bar.py`: Tinh gọn thanh tìm kiếm & thay thế.
- `src/ui_flet/views/help_view.py`: Tái thiết kế Card Grid 2 cột và FAQ Accordion.
- `src/ui_flet/views/settings_view.py`: Chuẩn hóa Typography Scale và điều hướng cài đặt.
- `src/ui_flet/views/editor_view.py` & `preview_view.py`: Căn chỉnh Header panel và divider.
- `src/ui_flet/native_dialogs.py`: Bổ sung `pick_directory_sync/async`.
- `src/i18n/locales/vi.json` & `en.json`: Cập nhật key từ điển và dọn sạch emoji văn bản.
- `tests/test_simplified_ribbon.py`: Bộ test suite kiểm thử toàn diện v1.7.0.
- `README.md`, `docs/roadmaps/product_roadmap.md`, `docs/roadmaps/studio_workspace_roadmap.md`.
